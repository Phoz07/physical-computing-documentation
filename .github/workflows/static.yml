name: Deploy to GitHub Pages

on:
  # Trigger เมื่อมีการ push ไปยัง branch main
  push:
    branches: [ main ]
  
  # หรือสามารถ trigger manually ได้จากหน้า Actions
  workflow_dispatch:

# ตั้งค่า permissions สำหรับ GITHUB_TOKEN
permissions:
  contents: read
  pages: write
  id-token: write

# ป้องกันไม่ให้มี concurrent deployments
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout code
      - name: Checkout
        uses: actions/checkout@v4
      
      # 2. Setup pnpm (อ่าน version จาก package.json หรือใช้ latest)
      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9
      
      # 3. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      
      # 4. Install dependencies
      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile
      
      # 5. Install Sharp for image optimization
      - name: Install Sharp
        run: pnpm add sharp
      
      # 6. Build Astro site
      - name: Build with Astro
        run: pnpm run build
      
      # 7. Upload artifact สำหรับ deployment
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4